package extendsthreadclass;

public class MainClass {
    public static void main(String[] args) {
        SampleThread sampleThread = new SampleThread(); // create sample thread
        DemoThread demoThread = new DemoThread(); // create demo thread
    }
}
